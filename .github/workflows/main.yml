name: CI

on: [push, pull_request]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: awalsh128/cache-apt-pkgs-action@latest
        with:
          packages: fpc
          version: 1.0
      - name: Build
        run: fpc tests.pas
      - name: Run tests
        run: ./tests
